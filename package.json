{
 "name": "api-jogador-time",
 "version": "1.0.0",
 "description": "API fake com JSON Server para projeto Flutter",
 "scripts": {
   "start": "json-server --watch db.json --port $PORT"
 },
 "dependencies": {
   "json-server": "^0.17.3"
 }
}
